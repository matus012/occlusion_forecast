N1 STATUS — 2026-07-26 (kickoff session 1; Phase 0 scaffold)

STATE: Phase 0 COMPLETE (pending push verify). Phase 1 (eval-kit sanity, G-N1-0) starting.
Ship target Sept 12.

DONE THIS SESSION:
- git init; default-deny .gitignore (data/, cache/, checkpoints/, *.parquet, all visuals);
  handoff prompt kept local-only.
- License guard ported from P1 D37/D41: per-file visual allowlist (empty), blob-ext +
  2MB size-ceiling default-deny, AV2 parquet explicitly denied. Green on staged tree.
- gates.yaml G-N1-0..4 (all UNFROZEN, provisional numerics annotated) + live G_quality;
  scripts/check_gates.py ported (live-gate flag instead of hardcoded name).
- tests: license guard (6) + gates-config sanity (4) — 10 pass; ruff clean.
- uv venv py3.11 pinned: torch 2.13.0+cu126 (CUDA OK on 4060), av2, pytorch-lightning,
  pytest/ruff toolchain; requirements.txt frozen (76 pins).
- CI workflow: torch-free guard subset (ruff + license guard + gates-config).
- LICENSE Apache-2.0 (D-N1-3); README stub with honesty labels; decision log D-N1-3/4.
- Reviewer pass done (fresh-context subagent): 2 blockers fixed (requirements.txt local
  path, status wording), branch renamed main. Remote link + push = final step of this
  session (URL pre-approved: github.com/matus012/occlusion_forecast).

OPEN (owner: Matúš; batched, non-blocking):
1. RESOLVED: repo URL = github.com/matus012/occlusion_forecast.
2. RESOLVED: license = Apache-2.0 (D-N1-3).
3. Confirm PERUN request covers ~O(100) H200h N1 ceiling, or amend.
4. P2 occlusion-stats export: machine-readable segment-stats JSON already emitted by P2,
   or extract from P1 repo results/ in wk1? (Claude will attempt extraction either way.)
5. AV2 download location/quota: ~60GB+ motion forecasting split — default plan is
   data/av2/ on this drive; flag if it should live elsewhere.

PHASE 1 PROGRESS (this session):
- forecast-mae vendored (untracked) @ cb86ea9, pinned in results/fmae_vendor_manifest.json.
- FINDING (D-N1-5): FMAE has NO declared license -> never redistributable; wrap-and-import
  only. Published val numbers read from pinned README -> results/fmae_published_val.json
  (fine-tune 0.7117/1.408/0.178; scratch 0.7214/1.430/0.187).
- FINDING (D-N1-6): all 3 FMAE checkpoint links DEAD (OneDrive 404; no mirrors anywhere).
  Adaptation: G-N1-0 eval-kit sanity re-anchored to QCNet released checkpoint (live,
  Apache-2.0, val 0.72/1.25/0.16 -> results/qcnet_published_val.json). FMAE stays the
  training baseline. QCNet ckpt downloaded (93MB, SHA256 in results/qcnet_ckpt_manifest.json).
- AV2 val split sync running in background (aws s3 --no-sign-request, public bucket,
  data/av2/val/, ~25k scenarios); train split queued next.

G-N1-0: PASSED + FROZEN (D-N1-8, 2026-07-26). QCNet ckpt through official av2 kit,
all 24988 val scenarios: 0.7201/1.2527/0.1574 vs published 0.72/1.25/0.16 — deltas
<= 0.003, frozen tolerance 0.01. Eval path validated. AV2 val verified (24988 dirs,
0 structural errors, SHA manifested). G-N1-1 freeze deferred to D-N1-7 decision.

NEXT (autonomous):
1. Finish AV2 train split sync (running, hours; resumable).
2. FMAE preprocess + overfit-100 smoke in main venv (Phase 2 start) — FMAE still
   provisional baseline pending D-N1-7; smoke work is wrap-and-import only.
3. P2 occlusion segment stats extraction (Phase 3 prep, baseline-agnostic).
4. Wk1 D-N1-7 comparison report (FMAE grant vs QCNet vs SIMPL) — user decides.

TRIPWIRE (D-N1-7, user directive 2026-07-26): FMAE-as-training-baseline is PROVISIONAL.
End of wk1: (a) license grant on the FMAE issue -> keep FMAE; (b) else cost out
QCNet-switch vs own-code FMAE-finetune reimplementation (honest H200h + dev-time box),
report comparison here with recommendation; USER DECIDES. Masking engine stays
baseline-agnostic either way.
- FMAE issue FILED: github.com/jchengai/forecast-mae/issues/25 (checkpoints + license).
- PRELIMINARY cost-out data (lit-based, to be replaced by measurement):
  QCNet: README states ~160GB GPU mem across 8x3090, batch 32; community-reported
  multi-day training -> rough order 150-250 H200h per full train x 9 runs (3 arms x
  3 seeds) = 1.4-2.2k H200h — ~15-20x OVER the O(100) ceiling. Even a cut matrix
  (2 arms x 2 seeds) likely blows budget. QCNet-switch is only viable with a
  drastically reduced claim scope.
  FMAE: hours-scale on 4 GPUs -> O(10) H200h/run, matrix fits ceiling (as planned).
  Own-code FMAE-finetune reimpl: dev-time risk (transformer encoder + pipeline from
  paper spec), compute cost same as FMAE.
  SIMPL (option 3, D-N1-7a, verified 2026-07-26): MIT license; AV2 ckpt SHIPPED IN-REPO
  (no link rot; SHA256 manifested); ckpt-matched val numbers author-posted (0.777/1.452/
  0.196 — '-fix' retrain, paper original lost); per-timestep validity ingestion CONFIRMED
  (PAD_OBS channel in actor features); plain PyTorch, no PyG deps; est. O(10-30) H200h/run
  -> 9-run matrix ~100-270 H200h. Caveat: native convention nn-pads invalid positions
  (flag+nn-pad vs FMAE flag+zero) — flag-flip is the cross-baseline masking invariant.
  Leaning (1-line): SIMPL is now the strongest no-license-grant option (in-repo ckpt
  anchors G-N1-0/1, budget fits, masks plug in natively); full report end of wk1.

BLOCKERS: none. (Push resolved — user pushed; origin/main tracking set.)
