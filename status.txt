N1 STATUS — 2026-07-27 (session 2: D-N1-14 HPC committee package)

STATE: D-N1-7 RESOLVED (baseline = SIMPL, D-N1-13). HPC package (D-N1-14) in
flight: C1-local training RUNNING (bg, ~5.5h, 20k x 20ep bs8 seed42); C3-local
queued after it. Eval/curve/video/report harnesses BUILT + committed; eval path
mechanically verified (masks reach model inputs). Ship target Sept 12.

DONE THIS SESSION:
- D-N1-13: baseline = SIMPL (user decision); context.md/status updated; FMAE
  demoted to license-grant-AND-checkpoints-republished contingency.
- D-N1-14a budget freeze BY MEASUREMENT (results/local_budget.json): bs16/bs12
  spill 8GB VRAM (measured, 3-13 samp/s thrash); bs8 = 5GB/21 samp/s steady.
  Local arms: 20k train (city-stratified prefix) x 20 epochs, dev 1k disjoint,
  val subset 2.5k (pattern-cohort-stratified prefix), final-epoch ckpts, 1 seed.
- Deterministic subset orderings + committed manifest (results/
  local_subsets_manifest.json); full id lists local (results/local/, gitignored).
- SIMPL features preprocessed: 21k train + 2.5k val (25-27 scen/s, 10 workers,
  137KB/scen; 17 transient MemoryError failures backfilled — root cause:
  concurrent training run; op rule adopted: never overlap train + preprocess).
- otraj.masking.simpl_apply: native-convention occlusion application
  (ffill/bfill parity, obs-window-only sources = no future leakage) + C2
  CV/linear imputation; 13 unit tests, torch-free, all green.
- scripts/: preprocess_local, train_local (C1/C3 arms, occl-aug via
  draw_train_mask per epoch), eval_local (av2 kit authority, per-scenario
  records + per-pattern slices), degradation_curve (watermarked headline +
  per-pattern figs, bootstrap CIs, paired-delta summary), inference_video
  (side-by-side C1 vs C3, identical masks, hidden-GT markers, auto-pick,
  labeled illustrative), build_perun_report (every number from results/*.json,
  Chrome-headless PDF), _simpl_compat (py3.11/torch-2.13 shims, zero
  third_party edits).
- reports/perun_request/template.md committed (text-only carve-out in
  .gitignore); PDF + figures stay local.

RUNNING / NEXT (autonomous):
1. C1-local train (bg) -> verify dev curve sane; spot-check S0-vs-S3 output
   sensitivity of the trained ckpt before full eval.
2. C3-local train (launch after C1; GPU serial).
3. eval_local x {C1 native, C2 impute on C1 ckpt, C3 native} x S0-S4 R-A
   on the fixed 2.5k val subset -> degradation_curve -> inference_video
   (S3/M1 x2 + S4 take).
4. build_perun_report -> request.md + request.pdf; reviewer pass (fresh-context
   subagent) on number traceability; then notify user for edit+submit.

BATCHED QUESTIONS (non-blocking; will proceed with stated defaults):
1. H200 conversion band default 6-10x per-GPU vs laptop 4060 (stated as an
   assumption; pilot calibration run included in the request). Override if
   PERUN guidance dictates a different planning factor.
2. Report attaches curve figures + video stills INSIDE the local PDF only;
   public repo carries text-only request.md. Confirm this split is what you
   want for the committee submission.
3. Storage ask defaults to ~100GB scratch (raw AV2 60GB + full-split SIMPL
   features ~31GB + ckpts). Adjust if PERUN quotas prefer a different shape.

BLOCKERS: none.
