N1 STATUS — 2026-07-27 (session 2 END: D-N1-14 HPC committee package READY)

STATE: Package COMPLETE, awaiting owner edit + submission. Both *-local arms
trained (6 epochs each, matched, D-N1-14b truncation), eval matrix + curve +
video + PERUN request done; adversarial reviewer pass applied (D-N1-14c).
Ship target Sept 12 unchanged.

DELIVERABLES (all local paths):
- reports/perun_request/request.pdf  <- EDIT + SUBMIT THIS (figures embedded)
- reports/perun_request/request.md   (tracked, text-only twin)
- reports/local_curves/degradation_curve_local.png (+ per-pattern appendix)
- reports/videos/*.mp4|gif + stills/ (2 takes, M1/S3/R-A, labeled illustrative)
- Regenerate everything: scripts/build_perun_report.py (numbers only from
  results/*.json), scripts/degradation_curve.py, scripts/inference_video.py.

HEADLINE NUMBERS (all *-local, 1 seed, 6 epochs, val subset 1500, R-A):
- C1-local degrades minFDE6 5.481 -> 6.970 (+27%) S0->S4; MR6 0.60 -> 0.76.
  The robustness gap (project motivation) is demonstrated at reduced scale.
- C2-local (CV imputation on C1 ckpt; NOT a third training run) flattens the
  curve to clean level for this under-trained model (max dev 0.007 m).
- C3-local COLLAPSED to history-invariant (D-N1-14c; verified 1e-5 m output
  sensitivity vs 65.7 m for C1; results/local/c3_collapse_verification.json).
  Honestly reported in report/curve/video; C3-vs-C1 unanswerable at laptop
  scale -> strengthens the HPC case; full protocol gains collapse monitoring.
- PERUN ask: 260 H200h + ~2 CPU-node-h + 100 GB scratch (post-review numbers;
  basis = frozen 21.0 samp/s from results/local_budget.json, H200 band 6-10x
  stated as assumption with 5 H200h pilot calibration).

REVIEW RECORD: fresh-context opus reviewer, 14 findings, 3 BLOCKERs (all one
root cause: best-epoch throughput substituted for frozen steady basis ->
under-asked 30%). All BLOCKERs + MINORs fixed, rebuild verified. Honesty
labels audited clean (truncation on every surface, collapse reported,
single-seed framing, *-local never conflated with gate arms).

TIME-BOX (D-N1-14b): user capped ~9h; epoch-13 target unreachable (snapshots
only at 5/10/15 + 12-158 min epoch variance from host throttle episodes);
both arms truncated at epoch 5 = 6 epochs, matched. Slight cap overrun due to
C3's throttled 7.7h wall — trains/eval/report all inside; discovered-work
(collapse diagnosis + reviewer fixes) added ~40 min.

BATCHED QUESTIONS (non-blocking, defaults applied):
1. H200 band 6-10x + pilot: override only if PERUN guidance dictates.
2. PDF embeds AV2-derived figures and stays LOCAL/untracked; public repo has
   text-only request.md. Flip only if the committee needs a repo link to the
   PDF (then we need a license call on the stills).
3. Storage ask 100 GB / CPU ~2 h: adjust to PERUN quota shapes if needed.
4. C3 collapse follow-up for the REAL matrix: proposed mitigations to test on
   HPC (lower LR for aug arm is NOT allowed by matched-arms rule — instead:
   full 50-epoch schedule with anneal, 3 seeds, collapse monitoring, optional
   curriculum p_occ ramp as a pre-registered variant). Flag if you want the
   curriculum variant dropped/kept before the full matrix spec freezes.

NEXT SESSION (after owner submits):
1. paired_test.py (G-N1-2 implementation, Phase 4 pattern).
2. R-B eval-path anchor caveat design (TRAJS_CTRS leakage note, D-N1-14 docs).
3. PERUN pilot run spec (1-epoch calibration + collapse monitor hooks).

BLOCKERS: none. Package awaits owner.
